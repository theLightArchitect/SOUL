{
  "mcpServers": {
    "SOUL": {
      "command": "${CLAUDE_PLUGIN_ROOT}/servers/soul",
      "env": {
        "RUST_LOG": "info",
        "SOUL_ALLOW_WRITE": "true",
        "ELEVENLABS_API_KEY": "${ELEVENLABS_API_KEY}"
      }
    }
  }
}
